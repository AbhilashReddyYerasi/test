{% from 'macros/shared_v1.j2' import create_table_and_stream -%}

{% call create_table_and_stream(
    cms_schema,
    load_cms_schema,
    "tax_deductions",
    "cms_stage",
    "TaxDeductions"
  ) -%}
    id varchar,
    transport_order_id varchar,
    shipment_number varchar(255),
    carrier_id varchar,
    carrier_cost_id varchar,
    cost_amount number(16,2),
    tax_rate number(16,6),
    tax_amount number(16,2),
    purchase_order_id varchar,
    purchase_order_number varchar(255),
    is_deleted boolean,
    created_by varchar,
    created_at timestamp_tz(9),
    updated_by varchar,
    updated_at timestamp_tz(9),
    tenant_id varchar,
    tenant_country_id varchar,
    tax_deduction_type varchar,
    currency_code varchar(50),
    currency_code_id varchar,
    master_countries_country_time_zone varchar(16777216)
{%- endcall %}
