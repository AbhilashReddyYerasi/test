{% from 'macros/shared_v1.j2' import create_table_and_stream -%}

{% call create_table_and_stream(
    cms_audit_schema,
    load_cms_audit_schema,
    "offer_item_histories",
    "cms_audit_stage",
    "offer_item_histories"
  ) -%}
    id number(38,0),
    offer_history_id varchar,
    action_type varchar,
    trigger_from varchar,
    rate_id varchar,
    rate_type varchar,
    carrier_id varchar,
    buying_rate number(16,2),
    selling_rate number(16,2),
    is_cbt_rate boolean,
    reference_ids varchar,
    inserted_at timestamp_tz(9),
    inserted_by varchar,
    created_by varchar,
    created_at timestamp_tz(9),
    tenant_country_id varchar,
    master_countries_country_time_zone varchar
{%- endcall %}
